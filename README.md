# MNIST

Basic Tensorflow Neural Network using the MNIST dataset
The training data I used is from the Kaggle challenge for image classification
and I just tested on the MNIST data that's built into tensorflow.
